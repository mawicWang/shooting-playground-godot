## Commit Checklist
1. Run `git status` and verify ALL changed files are staged
2. Check no previously working features were removed
3. Run build if applicable
4. Write concise commit message in Chinese
5. Push to current branch
